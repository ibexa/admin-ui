name: Frontend build

on:
    push:
        branches:
            - '[0-9]+.[0-9]+'
    pull_request: ~

jobs:
    frontend-test:
        name: Frontend build test
        runs-on: 'ubuntu-26.04'
        timeout-minutes: 5

        steps:
            - uses: actions/checkout@v7
            - uses: actions/setup-node@v7
              with:
                  node-version: '18'
            - run: yarn install
            - run: yarn test
