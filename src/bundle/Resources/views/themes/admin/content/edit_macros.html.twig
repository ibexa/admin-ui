{% macro render_form_field(formField, attr) -%}
    {% trans_default_domain 'ibexa_content_edit' %}

    {% if not formField.rendered %}
        {% if formField.value is defined %}
            {{- form_widget(formField, { 'attr': attr }) -}}
        {% else %}
            <div class="ibexa-field-edit ibexa-form-field ibexa-field-edit--eznoneditable">
                {{- form_label(formField) -}}
                <p class="non-editable">
                    {{ 'content.field.non_editable'|trans|desc('This Field Type is not editable') }}
                </p>
                {% do formField.setRendered() %}
            </div>
        {% endif %}
    {% endif %}
{%- endmacro -%}

{% macro render_field_group(group, form, attr) -%}
    {% for field in group %}
        {% set formField = form.fieldsData[field] %}

        {{ _self.render_form_field(formField, attr) }}
    {% endfor %}
{%- endmacro -%}
